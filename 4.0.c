#include <stdio.h>

int main(void) {

    unsigned int i = 0;
    unsigned int j = 0;
    unsigned int product = 0;

    for (i = 100; i <= 999; i++) {
        for (j = 100; j <= 999; j++) {
            product = i * j;
            // check if number reads forward and backward the same
            // 1001
            // 21012 

            // BRAINSTORM 1
            // check how big number is through division or modulus
            //     can only be 6 or 7 digits
            // store length of number in variable

            //
    
        }
    }
    return 0;
}
